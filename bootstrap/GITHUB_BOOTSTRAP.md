# GitHub bootstrap handoff

Recommended repository name: `MOSAIC-ODE`.

Recommended initial branch layout:

- `main`: bootstrap source after review;
- `develop`: optional integration branch after the first serious development cycle;
- feature branches for vector, CUDA, topology, homotopy, and online-learning work.

The bootstrap repository should be uploaded only after the local proof and independent review gates pass. Do not advertise GPU speedup, broad production universality, or calibrated Bayesian uncertainty from this pre-product package.
